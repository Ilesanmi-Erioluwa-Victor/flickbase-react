const { authService } = require("../services");

const authController = {
  async register() {
    try {
    } catch (error) {
      console.log(error);
    }
  },
};

module.exports = authController;
