// Models
const { User } = require("../models/user");

const createUser = async (email, password) => {
  try {
    // 1. Check if user email doesn't exist
  } catch (error) {}
};

module.exports = {
  createUser,
};
